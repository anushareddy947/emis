version: '3.4'
services:
  db:
    build: .
    image: mysql:8.0.2
    environment:
      MYSQL_ROOT_PASSWORD: 'abc'
      database: 'json_data'
    ports:
      - '3309:3306'
  phpmyadmin:
    image: phpmyadmin
    restart: always
    ports:
      - 8080:80
    environment:
      - PMA_ARBITRARY=1